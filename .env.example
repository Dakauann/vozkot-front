NEXT_PUBLIC_APP_NAME=Vozkot
NEXT_PUBLIC_API_URL=http://localhost:8080
# Public hostname the ticket media is served from in production (the Cloudflare
# R2 public URL). Leave empty locally: media then comes from the API itself.
NEXT_PUBLIC_MEDIA_URL=
